javascript:(function(){var%20titleInput=document.getElementById('pull_request_title');var%20bodyInput=document.getElementById('pull_request_body');function%20extractTicketAndFormatTitle(title){let%20ticketParts=title.split('/')[1].split('%20').slice(0,2);let%20ticketPrefix=ticketParts[0].toUpperCase();let%20ticketNumber=ticketParts[1];let%20description=title.split('/')[1].split('%20').slice(2).join('%20');description=description.replace(/\b\w/g,l=>l.toUpperCase());const%20formattedTitle=`[Commit%20Prefix]%20${ticketPrefix}-${ticketNumber}:%20${description}`;titleInput.value=formattedTitle;return{ticketPrefix,ticketNumber};}function%20formatTitleAndBody(){const{ticketPrefix,ticketNumber}=extractTicketAndFormatTitle(titleInput.value);const%20jiraUrl=`https://ergatta.atlassian.net/browse/${ticketPrefix}-${ticketNumber}`;const%20formattedBody=`Jira:%20${jiraUrl}\n\n#%20Description\n\n${bodyInput.value}`;bodyInput.value=formattedBody;}formatTitleAndBody();})();