javascript:(function(){var titleInput=document.getElementById('pull_request_title');var bodyInput=document.getElementById('pull_request_body');var branchSelectors=document.getElementsByClassName("branch");function isNumeric(str){return /^\d+$/.test(str);}function extractTicketAndFormatTitle(title){let branchNameNoFleet="";let branchParts=[];if(branchSelectors[1].innerText.includes("compare:")){let branchName=branchSelectors[1].innerText.replace("compare: ","");branchNameNoFleet=branchName.split('/')[1];branchParts=branchNameNoFleet.split('-');}else if(title.includes("/")){let branchName=title;branchNameNoFleet=branchName.split('/')[1];branchParts=branchNameNoFleet.split(' ');}let ticketParts=branchParts.slice(0,2);let ticketPrefix='';let ticketNumber='';let description='';if(isNumeric(ticketParts[1])){ticketPrefix=ticketParts[0].toUpperCase();ticketNumber=ticketParts[1];description=branchParts.slice(2).join(' ');}else{description=branchNameNoFleet.replaceAll("-"," ");}description=description.replace(/\b\w/g,l=>l.toUpperCase());const formattedTitle=ticketPrefix&&ticketNumber?`[Commit Prefix] ${ticketPrefix}-${ticketNumber}: ${description}`:`[Commit Prefix] ${description}`;return{formattedTitle,ticketPrefix,ticketNumber};}function formatTitleAndBody(){let formattedBody='';const{formattedTitle,ticketPrefix,ticketNumber}=extractTicketAndFormatTitle(titleInput.value);titleInput.value=formattedTitle;if(ticketPrefix&&ticketNumber){const jiraUrl=`https://ergatta.atlassian.net/browse/${ticketPrefix}-${ticketNumber}`;formattedBody=`Jira: ${jiraUrl}\n\n# Description\n\n${bodyInput.value}`;}else{formattedBody=`# Description\n\n${bodyInput.value}`;}bodyInput.value=formattedBody;}formatTitleAndBody();})();