javascript:(function(){var titleInput=document.getElementById('pull_request_title');var bodyInput=document.getElementById('pull_request_body');function isNumeric(str){return /^\d+$/.test(str);}function extractTicketAndFormatTitle(title){let secondPart=title.split('/')[1];let ticketParts=secondPart.split(' ').slice(0,2);let ticketPrefix='';let ticketNumber='';let description='';if(isNumeric(ticketParts[1])){ticketPrefix=ticketParts[0].toUpperCase();ticketNumber=ticketParts[1];description=secondPart.split(' ').slice(2).join(' ');}else{description=secondPart;}description=description.replace(/\b\w/g,l=>l.toUpperCase());const formattedTitle=ticketPrefix&&ticketNumber?`[Commit Prefix] ${ticketPrefix}-${ticketNumber}: ${description}`:`[Commit Prefix] ${description}`;return {formattedTitle,ticketPrefix,ticketNumber};}function formatTitleAndBody(){let formattedBody='';const {formattedTitle,ticketPrefix,ticketNumber}=extractTicketAndFormatTitle(titleInput.value);titleInput.value=formattedTitle;if(ticketPrefix&&ticketNumber){const jiraUrl=`https://ergatta.atlassian.net/browse/${ticketPrefix}-${ticketNumber}`;formattedBody=`Jira: ${jiraUrl}\n\n# Description\n\n${bodyInput.value}`;}else{formattedBody=`# Description\n\n${bodyInput.value}`;}bodyInput.value=formattedBody;}formatTitleAndBody();})();